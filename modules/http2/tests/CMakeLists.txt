add_executable(mira_test_http2 test_session.cpp)
target_link_libraries(mira_test_http2 PRIVATE Mira::http2 Mira::warnings NGHTTP2::NGHTTP2)
target_include_directories(mira_test_http2 PRIVATE ${PROJECT_SOURCE_DIR}/modules/core/tests)
add_test(NAME mira_http2 COMMAND mira_test_http2)
set_tests_properties(mira_http2 PROPERTIES TIMEOUT 30)

add_executable(mira_test_http2_network test_network.cpp)
target_link_libraries(mira_test_http2_network PRIVATE Mira::http2 Mira::transport Mira::warnings)
target_include_directories(mira_test_http2_network PRIVATE ${PROJECT_SOURCE_DIR}/modules/core/tests)
if(MIRA_ENABLE_TLS)
    target_link_libraries(mira_test_http2_network PRIVATE Mira::tls)
    target_compile_definitions(mira_test_http2_network PRIVATE MIRA_HTTP2_TEST_TLS=1
        MIRA_HTTP2_TEST_BINARY_DIR="${CMAKE_CURRENT_BINARY_DIR}")
endif()
add_test(NAME mira_http2_network COMMAND mira_test_http2_network)
set_tests_properties(mira_http2_network PROPERTIES TIMEOUT 45)
